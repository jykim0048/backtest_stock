name: Daily Market Report

on:
  schedule:
    # 16:00 KST = 07:00 UTC  — runs right after KRX market close (Mon–Fri)
    - cron: '0 7 * * 1-5'
  workflow_dispatch: # allow manual trigger at any time

jobs:
  generate-report:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate daily report
        run: python generate_report.py

      - name: Commit and push report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add daily_market_report.json reports/
          git diff --staged --quiet && echo "No changes to commit" && exit 0
          git commit -m "chore: auto-generate daily market report $(date +'%Y-%m-%d')"
          git push
